window.rcmail && rcmail.addEventListener('init', zoner_2fa_login);

function zoner_2fa_login() {
    $('#zoner_2fa-device-remember').on('change', function () {
        $('#zoner_2fa-device-name').prop('disabled', !this.checked);
    });
}